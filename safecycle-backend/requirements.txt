fastapi>=0.115.0
uvicorn>=0.30.0
python-dotenv>=1.0.0
pytest>=8.0.0
httpx>=0.27.0
anthropic>=0.40.0
# Google ID token verification (Phase 1 - strict Google sign-in).
google-auth>=2.30.0
# Required by google.auth.transport.requests for fetching Google's public keys.
requests>=2.32.0
# Supabase Python client (Phase 1 - persist users; Phase 2 - sessions + history).
supabase>=2.5.0
